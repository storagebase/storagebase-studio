/**
 * The vault workbench barrel (StorageBase fork). The shell imports the
 * workbench from here; the tabs, forms and panels are internal to it.
 */
export { VaultWorkbench } from "./VaultWorkbench";
