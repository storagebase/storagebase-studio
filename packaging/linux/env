# Environment for the storagebase-studio systemd service (issue #112).
# Loaded by the unit via EnvironmentFile=-/etc/storagebase-studio/env.
# Format: KEY=value, one per line (no "export", no shell expansion).
# Apply changes with: systemctl restart storagebase-studio
#
# Port and bind address (defaults: 3000, loopback only - set
# HOSTNAME=0.0.0.0 to expose the service on the network).
#PORT=3000
#HOSTNAME=127.0.0.1
#
# Authentication. When unset, the zero-config first run generates JWT_SECRET
# and ADMIN_PASSWORD, persists them in
# /var/lib/storagebase-studio/auth-bootstrap.json, and prints the admin password
# once to the journal: journalctl -u storagebase-studio
# Set AUTH_BOOTSTRAP=off to require explicit values instead.
#AUTH_BOOTSTRAP=off
#JWT_SECRET=change-me-to-a-random-32-char-string
#ADMIN_EMAIL=admin@storagebase.org
#ADMIN_PASSWORD=
#USER_EMAIL=user@storagebase.org
#USER_PASSWORD=
#
# Two-factor authentication (TOTP), optional and per account. Base32, at least
# 26 characters; generate one with: openssl rand 20 | base32
# A value that is not base32, or too short, stops login with a 503 naming the
# variable. Enrolment and recovery: docs/MFA.md
#ADMIN_TOTP_SECRET=
#USER_TOTP_SECRET=
#
# AI query assistance (optional).
#LLM_PROVIDER=gemini
#LLM_API_KEY=
#LLM_MODEL=gemini-2.5-flash
