# Run StorageBase Studio on Fly.io from the prebuilt image.
# Setup steps: docs/FLY.md
#
# Credentials are NOT set here on purpose - put them in Fly secrets
# (see docs/FLY.md), never in a committed file.

app = 'storagebase-studio'
primary_region = 'ams'

[build]
  image = 'ghcr.io/storagebase/storagebase-studio:0.14.1'

[env]
  NEXT_PUBLIC_AUTH_PROVIDER = 'local'
  NODE_ENV = 'production'
  PORT = '3000'
  STORAGE_PROVIDER = 'sqlite'
  STORAGE_SQLITE_PATH = '/app/data/storagebase-storage.db'

[mounts]
  source = 'storagebase_data'
  destination = '/app/data'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  # SQLite on a single volume: keep this at one machine, don't scale out.
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '20s'
    method = 'GET'
    path = '/api/db/health'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
